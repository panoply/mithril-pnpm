{
  "name": "@mpnpm/prettier-config",
  "version": "1.2.1",
  "description": "Shared prettier config across monorepo",
  "main": "prettier.cjs",
  "prettier": "./prettier.cjs",
  "license": "MIT",
  "keywords": [
    "prettier",
    "prettierconfig",
    "config",
    "formatting"
  ],
  "scripts": {
    "bump": "pnpm up @mpnpm/prettier-config -r",
    "postversion": "pnpm bump"
  },
  "engines": {
    "pnpm": ">=6"
  },
  "devDependencies": {
    "prettier": "^2.4.1"
  },
  "peerDependencies": {
    "prettier": "^2.4.1"
  }
}
